<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enrserviceapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ServiceRequestActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".IntroActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".FinalOrderConfirmationActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity
            android:name=".AddMapLocationActivity"
            android:label="@string/title_activity_add_map_location" />
        <activity android:name=".ExportToExcelActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".WorkCompletedActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <!--
            The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key
             that is used to sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".TimerActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".OrderPlacingActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".AboutActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".ServicesActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".CreateAccountActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".InitialActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden">
            <!-- // Intent filter to be started from here !! -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>